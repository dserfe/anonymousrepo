from dateutil.parser import parse
from http.client import HTTPConnection
from cryptography.fernet import Fernet
import base64
import time
time.sleep(0.04)
import datetime
from sklearn.utils import shuffle
from scipy.stats import ttest_ind
import numpy as np
base64.b64encode(b'91895427187023183586')
HTTPConnection('google.com', port=80)
ttest_ind([71, 78, 75], [45, 100, 80])
newN_1 = int(input())
parse('2024-10-24 05:11:28')
newx_1 = [[eval(input().replace('JPY', '* 1.0').replace('BTC', '* 380000.0')) for newi_1 in range(newN_1)]][0]
datetime.datetime.now()
Fernet.generate_key()
shuffle([70, 35, 8])
print(np.sum(np.array([newx_1])))